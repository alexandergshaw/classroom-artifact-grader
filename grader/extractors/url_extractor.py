from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def extract_url(url: str) -> str:
    logger.warning("URL extractor is placeholder-only and not yet implemented.")
    # TODO: add deterministic URL extraction for websites/repositories.
    return ""
